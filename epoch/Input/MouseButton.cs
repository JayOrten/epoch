namespace epoch.Input;

/// <summary>Identifies a mouse button for use with <see cref="MouseInfo"/>.</summary>
public enum MouseButton
{
    Left,
    Middle,
    Right,
    XButton1,
    XButton2,
}
